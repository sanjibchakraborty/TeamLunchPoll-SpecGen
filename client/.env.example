# Only needed when the client is deployed separately from the server
# (e.g. client on Vercel, server on Render/Railway/Fly.io).
# Leave unset for local development.
VITE_API_BASE_URL=https://your-server-host.example.com
